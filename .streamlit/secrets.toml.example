# Copy this to secrets.toml and fill in your credentials
# NEVER commit secrets.toml to git!
#
# Note: KBC_TOKEN is provided via the UI, not stored here
# Note: Workspace URL is now configured via the UI for SQL-level comparisons

KBC_URL = "https://connection.north-europe.azure.keboola.com"
